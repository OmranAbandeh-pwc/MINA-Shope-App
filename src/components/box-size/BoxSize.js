import './boxSize.css'

const BoxSize = ({height, width}) => {
    const style = {
        height: height,
        width: width,
    }
  return (
    <div style={style}>
      
    </div>
  )
}

export default BoxSize
